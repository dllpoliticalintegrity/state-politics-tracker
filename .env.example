# Supabase project for this site (created in Phase 2 — see docs/plan.md).
# The app boots without these; data queries only happen once a state is
# "live" in src/states/registry.ts.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
